providers = ["node"]

[variables]
NODE_VERSION = "22.12.0"
NPM_CONFIG_INCLUDE = "optional"
VITE_SITE_URL = "https://cv.dotka.xyz"

[phases.install]
cmds = ["npm ci --include=optional"]

[phases.build]
cmds = ["npm run build"]
